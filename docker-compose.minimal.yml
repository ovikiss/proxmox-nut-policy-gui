services:
  proxmox-nut-gui:
    image: ghcr.io/ovikiss/proxmox-nut-policy-gui:v1.0
    container_name: proxmox-nut-gui
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
